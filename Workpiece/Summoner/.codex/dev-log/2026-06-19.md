# 2026-06-19

- DEV-070: Stage Select Scene 버튼 이벤트가 `stageLoader(int)`를 호출하는 기존 연결과 맞도록 `StageSelectView.stageLoader(int)` 호환 메서드를 추가했다. 씬 asset은 수정하지 않았다.
- DEV-069: `SummonController`의 소환 후보 생성 책임을 `SummonDrawService`로 분리하고 `Assembly-CSharp.csproj`에 포함했다. 검색 검증은 통과했지만 `dotnet build`는 Unity 생성 프로젝트의 `project.assets.json` 복원 산출물 누락으로 컴파일 단계 전 실패했다.
- DEV-071: 소환 배치 분리를 검토했지만 실제 배치 규칙은 `Plate.SummonPlaceOnPlate()`에 있고 `SummonController` 호출부는 한 줄 위임이라 `SummonPlaceService`는 만들지 않기로 했다.
- DEV-072: `StageSelectView`와 `StageController`의 스테이지 이동 switch를 제거하고 `StageFlowController`로 위임했다. Stage Select 씬 직접 실행을 위해 `StageFlowController.GetOrCreate()`를 추가했다.
- DEV-073: `TurnController`의 public API와 serialized field를 유지한 채 private 턴 흐름 이름을 `StartPlayerTurn`, `ApplyEnemyTurnStartEffects`, `TryClearBattle`처럼 읽히는 순서로 정리했다. 검색 검증은 통과했지만 `dotnet build`는 기존과 같은 `project.assets.json` 누락으로 컴파일 전 실패했다.
- DEV-074: `TurnController`의 반복 타이밍 책임을 검토했지만 실제 규칙은 `Summon`, `StatusEffectController`, `AttackCooldownState`에 있고 현재는 호출 타이밍 연결 수준이라 Runner 분리는 보류했다.
- DEV-075: `BattleProgressController`의 `StageFlowController` 참조 확보를 `StageFlowController.GetOrCreate()`로 통일했다. 검색 검증은 통과했지만 `dotnet build`는 기존과 같은 `project.assets.json` 누락으로 컴파일 전 실패했다.
- DEV-076: 전투 결과 판정 호출 중복을 검토했다. `PlayerActionController`의 승리 판정은 플레이어 공격 직후 확인이고, `TurnController`의 승리 판정은 적 턴 시작 효과와 적 플레이트 정리 이후 플레이어 턴 시작 시점 재확인이라 같은 책임의 단순 중복으로 보지 않았다. `BattleResultController.resultStarted`가 결과 표시 중복을 막고 있어 코드 변경 없이 유지한다.
- DEV-077: `EnermyAlgorithm`에서 실제 공격 실행 책임을 `EnemyAttackExecutor`로 분리했다. `EnermyAlgorithm`은 대응 공격 선택 흐름을 유지하고, 일반/강공격/특수공격 실행은 `EnemyAttackExecutor`가 담당한다. 검색 검증은 통과했지만 `dotnet build`는 기존과 같은 `project.assets.json` 누락으로 컴파일 전 실패했다.
- DEV-077-Folder: `EnemyAttackExecutor.cs`와 `.meta`를 `Battle/EnemyAction/Execution`으로 함께 이동해 실행 책임 폴더를 먼저 정리했다. `Assembly-CSharp.csproj`와 정적 테스트의 파일 경로를 새 위치로 갱신했고 검색 검증은 통과했다.
- DEV-078: `.codex` active 문서와 `DevAgent` 중단 조건을 현재 자동 진행 지시에 맞게 정리하고, `EnermyAlgorithm`의 예측 대응 특수공격 선택 반복을 `EnemySpecialAttackPicker`로 분리했다. `EnermyAlgorithm`은 대응 흐름과 실행 요청을 유지하고, `Picker`는 특수공격 후보 선택을 담당한다. 검색 검증은 통과했지만 `dotnet build`는 기존과 같은 `project.assets.json` 누락으로 컴파일 전 실패했다.
- DEV-079: `HandleReactNormalAttack()` 안에 남은 일반 대응 특수공격 선택 반복을 기존 `EnemySpecialAttackPicker`로 이관했다. `EnermyAlgorithm`에는 일반 대응 흐름과 fallback 일반 공격 실행만 남기고, 쿨타임/상태 타입 기반 특수공격 선택은 `Picker`가 담당한다. 검색 검증은 통과했지만 `dotnet build`는 기존과 같은 `project.assets.json` 누락으로 컴파일 전 실패했다.
- DEV-080: `EnermyAlgorithm`의 private 대응 라우팅 메서드 이름을 `TryReactWithSpecialAttack`, `TryReactToPoisonAllAttack`, `TryReactToTargetAttack`, `TryReactToAllAttack`, `TryReactToHealAttack`, `TryReactToUpgradeAttack`으로 정리했다. public API와 serialized field 변경 없이 private 이름만 변경했고 검색 검증은 통과했지만 `dotnet build`는 기존과 같은 `project.assets.json` 누락으로 컴파일 전 실패했다.
- DEV-081: `EnermyAttackController`의 피해 상태 힐 선사용 선택과 등급별 연속공격 확률 판단을 `EnemyActionPicker`로 분리했다. 컨트롤러는 적 행동 흐름과 선택된 회복 실행만 담당하고, 선택 판단은 `Picker`가 담당한다. 검색 검증은 통과했지만 `dotnet build`는 기존과 같은 `project.assets.json` 누락으로 컴파일 전 실패했다.
- DEV-082: `EnermyAlgorithm.HandleReactNormalAttack()`에 남아 있던 체력 차이 대상 인덱스 계산과 중급 이상 소환수 존재 판단을 `EnemyActionPicker`로 이관했다. `EnermyAlgorithm`에는 일반 대응 흐름과 실행 요청만 남겼고, 오래된 주석 처리 헬퍼도 제거했다. 검색 검증은 통과했지만 `dotnet build`는 기존과 같은 `project.assets.json` 누락으로 컴파일 전 실패했다.
- QA-083: `GetPlayerAttackPredictionsList()`의 예측 목록 생성 책임을 리뷰했다. `finally` 원복으로 런타임 Plate 임시 변경 안전장치는 있지만, `EnermyAlgorithm`이 대응 공격 선택과 예측용 플레이트 상태 준비/원복을 함께 담당하고 있어 `Needs Fix`로 판단했다. public 진입점은 유지하고 내부 예측 플레이트 준비 책임만 분리하는 DevAgent 작업으로 넘긴다.
- DEV-083: `EnemyPredictionPlateState`를 추가해 예측용 플레이어 플레이트 수집, 적 상태 적용, 런타임 Plate 원복 책임을 `EnermyAlgorithm`에서 분리했다. `GetPlayerAttackPredictionsList()` public 진입점과 `try/finally` 원복 흐름은 유지했다. 검색 검증은 통과했지만 `dotnet build`는 기존과 같은 `project.assets.json` 누락으로 컴파일 전 실패했다.
- DEV-084: `EnemyPredictionPlateState` 구조가 유지되도록 정적 회귀 기준을 보강했다. `EnermyAlgorithm`에 `Plate.SetCurrentSummon()` 기반 예측 상태 준비 책임이 다시 들어오지 않도록 검색 기준을 추가했다. 검색 검증은 통과했지만 `dotnet build`는 기존과 같은 `project.assets.json` 누락으로 컴파일 전 실패했다.
- QA-085: `EnemyAction/Picker`, `Execution`, `Prediction` 구조를 통합 리뷰했다. 분리 방향은 맞지만 `EnermyAlgorithm`에 특수공격 확률 선택 판단(`CanReactSpecialAttack`)이 남아 있어 `Needs Fix`로 판단했다. 해당 판단은 `EnemyActionPicker`로 이관한다.
- DEV-085: `EnermyAttackController -> EnermyAlgorithm -> PlateController`, `Enermy -> EnermyAttackController -> EnermyAlgorithm`, `TurnController -> Enermy -> EnermyAttackController -> PlateController` 형태의 건너건너 의존 체인을 제거했다. `EnermyAttackController`와 `TurnController`가 필요한 `PlateController`를 직접 참조하고 fallback을 두도록 바꿨으며, `Enermy`는 공격 컨트롤러만 직접 호출한다. 검색 검증은 통과했지만 `dotnet build`는 기존과 같은 `project.assets.json` 누락으로 컴파일 전 실패했다.
# 2026-06-19 Dev Log

## 진행 중

- QA-087: MCP `recompile_scripts`가 정상 연결됐고 0 warning으로 통과했다.
- QA-087: `Summoner.EditModeTests.PlateSummonDrawStaticRegressionTests.EnermyAttackController_DelegatesActionPicking` 통과.
- QA-087: `Summoner.PlayModeTests.StageRuntimeFlowPlayModeTests.FightScene_PlayerAndEnemyTurns_ExchangeTwoOrThreeTimes` 통과. Unity console error 로그는 비어 있었다.
- DEV-088: EnemyAction 역할 분리 첫 슬라이스로 `EnermyAttackController`의 직접 `SpecialAttack` 실행을 `EnemyAttackExecutor.ExecuteDirectSpecialAttack()` 책임으로 이동했다.
- DEV-088: MCP `recompile_scripts` 0 warning, `EnermyAttackController_DelegatesActionPicking` EditMode 통과, `FightScene_PlayerAndEnemyTurns_ExchangeTwoOrThreeTimes` PlayMode 통과. Unity console error 로그는 비어 있었다.
- QA-088: `EnermyAttackController`의 직접 `SpecialAttack()` 호출 제거와 `EnemyAttackExecutor.ExecuteDirectSpecialAttack()` 위임을 확인했다. 대상 컴파일/EditMode/PlayMode는 통과했다.
- DEV-089: `EnermyAlgorithm.CanReactSpecialAttack()` 확률 선택 판단을 `EnemyActionPicker.CanReactWithSpecialAttack()`으로 옮겼다.
- DEV-089: MCP `recompile_scripts` 첫 호출은 `Connection failed: Unknown error`였지만 Unity console error는 비어 있었고 재시도에서 0 warning으로 통과했다. `EnermyAttackController_DelegatesActionPicking` EditMode와 `FightScene_PlayerAndEnemyTurns_ExchangeTwoOrThreeTimes` PlayMode도 통과했다.
- QA-089: 특수공격 확률 선택 책임 이동을 확인했다. 대상 컴파일/EditMode/PlayMode는 통과했고 Unity console error 로그는 비어 있었다.
- DEV-090: 예측 데이터 타입 `AttackProbability`를 `EnermyAlgorithm.cs`에서 `Assets/Script/Battle/Prediction/AttackProbability.cs`로 분리했다. Unity가 `AttackProbability.cs.meta`를 생성했다.
- DEV-090: MCP `recompile_scripts` 첫 호출은 `Connection failed: Unknown error`였지만 Unity console error는 비어 있었고 재시도에서 0 warning으로 통과했다. `EnermyAttackController_DelegatesActionPicking` EditMode와 `FightScene_PlayerAndEnemyTurns_ExchangeTwoOrThreeTimes` PlayMode도 통과했다.
- QA-090: `AttackProbability` 데이터 위치 분리를 확인했다. 대상 컴파일/EditMode/PlayMode는 통과했고 Unity console error 로그는 비어 있었다.
- DEV-091: `EnermyAlgorithm.CanReactWithSpecialAttack()` private 인덱스 탐색 이름을 반환값이 드러나는 `FindSpecialAttackReactionIndex()`로 변경했다.
- DEV-091: MCP `recompile_scripts` 첫 호출은 `Connection failed: Unknown error`였지만 Unity console error는 비어 있었고 재시도에서 0 warning으로 통과했다. `EnermyAttackController_DelegatesActionPicking` EditMode와 `FightScene_PlayerAndEnemyTurns_ExchangeTwoOrThreeTimes` PlayMode도 통과했다.
- Coordinator: 다른 에이전트가 비슷한 EnemyAction 작업 중이라는 사용자 지시에 따라 추가 EnemyAction 구조개편을 중단하고 Save 도메인으로 전환했다.
- DEV-092: 새 게임 기본 진행값 생성을 `GameSaveData.CreateNewGameProgress()`로 모으고 `GameSaveController`의 중복 초기화 값을 제거했다.
- DEV-092: MCP `recompile_scripts` 첫 호출은 `Connection failed: Unknown error`였지만 Unity console error는 비어 있었고 재시도에서 0 warning으로 통과했다. `GameSaveController_UsesGameSaveDataFactoryForNewProgress` EditMode, `StartNewGame_ResetsProgressAndKeepsOptionPrefs` PlayMode, `ResetGameProgress_RemovesProgressAndKeepsOptionPrefs` PlayMode가 통과했다.
- QA-092: 새 게임 진행 데이터 생성 책임 분리를 확인했다.
- DEV-093: `AudioSettingStore`를 추가하고 `AudioSettingView`의 PlayerPrefs 직접 접근을 저장소 위임으로 바꿨다. Unity가 `AudioSettingStore.cs.meta`를 생성했다.
- DEV-093: MCP `recompile_scripts` 첫 호출은 `Connection failed: read ECONNRESET`였지만 Unity console error는 비어 있었고 재시도에서 0 warning으로 통과했다. `PlayerPrefsDirectAccess_StaysInsideKnownStorageFiles`, `AudioSettingView_DelegatesPlayerPrefsToStore`, `AudioSettingView_GuardsZeroVolumeBeforeLog10` EditMode 테스트가 통과했다.
- QA-093: 오디오 설정 저장 책임 분리를 확인했다.
- DEV-094: `VideoSettingStore`를 추가하고 `VideoSettingView`의 PlayerPrefs 직접 접근과 저장 인덱스 보정 책임을 저장소 위임으로 바꿨다. Unity가 `VideoSettingStore.cs.meta`를 생성했다.
- DEV-094: 최초 컴파일에서 `System.Collections.Generic` using 누락으로 `List<>` 컴파일 오류가 발생했고, using 복구 후 0 warning으로 통과했다. `PlayerPrefsDirectAccess_StaysInsideKnownStorageFiles`, `VideoSettingView_DelegatesPlayerPrefsToStore`, `VideoSettingView_RepairsInvalidSavedIndexesBeforeToggleSetup`, `VideoSettingView_RepairsInvalidSavedIndexesInPlayerPrefs`, `VideoSettingView_KeepsValidSavedIndexesInPlayerPrefs` EditMode 테스트가 통과했다.
- QA-094: 화면 설정 저장 책임 분리를 확인했다.
- DEV-095: `Option/Audio`, `Option/Video` 폴더를 만들고 Audio/Video View와 Store 파일을 `.meta`와 함께 이동했다. 테스트 경로와 PlayerPrefs 허용 경로를 새 구조에 맞게 갱신했다.
- DEV-095: `recompile_scripts` 0 warning, `PlayerPrefsDirectAccess_StaysInsideKnownStorageFiles`, `AudioSettingView_DelegatesPlayerPrefsToStore`, `VideoSettingView_DelegatesPlayerPrefsToStore` EditMode 테스트가 통과했다. Unity console error 로그는 비어 있었다.
- Coordinator: 실제 진행된 Battle 변경과 active 문서가 `Option/Video QA-095`로 어긋나 있어, 현재 슬라이스를 `QA-096 | Battle | EnemyAction 책임 분리와 명시 참조 연결 검증`으로 재정렬했다.
- QA-096: EnemyAction 책임 분리는 검색상 확인됐지만, FightScene의 `EnermyAttackController` 컴포넌트에 새 `plateController` serialized 값이 없어 `FindObjectOfType` 제거 후 null 차단 위험이 있다. 결과는 `Needs Fix`이며 DEV-096으로 넘긴다.
- DEV-096: FightScene 1~7의 `EnermyAttackController`에 `plateController`와 같은 GameObject의 `enermyAlgorithm` serialized 연결을 추가했다. `FindObjectOfType`와 `GetPlateController()` 체인은 되살리지 않았다. `dotnet build --no-restore`는 기존 `project.assets.json` 누락으로 환경 실패다.
- QA-097: FightScene 1~7의 `EnermyAttackController` 명시 참조와 EnemyAction 파일 포함/역할 분리를 검색으로 확인했다. `FindObjectOfType`와 getter 체인은 되살아나지 않았다. `dotnet build --no-restore`는 기존 `project.assets.json` 누락으로 환경 실패다.
- DEV-098: `Assets/Script/Battle/Prediction`과 `Battle/EnemyAction/Prediction/EnemyPredictionPlateState.cs`의 예측 함수에 학부생도 읽을 수 있는 수준의 `// 역할:` 주석을 추가했다. 로직, public API, serialized field, 씬/프리팹 값은 변경하지 않았다.
- DEV-098: `// 역할:` 검색 89건과 `git diff --check`를 확인했다. `git diff --check`는 오류 없이 CRLF 변환 안내만 출력했다.
- DEV-099: `Assets/Script/Battle/Prediction`과 `Assets/Script/Battle/EnemyAction` 범위의 클래스 속성에 간단한 `// 역할:` 주석을 추가했다. 소환수별 예측 클래스처럼 멤버 변수가 없는 클래스에는 별도 속성 주석을 추가하지 않았다.
- DEV-099: 클래스 속성 주석 누락 검사 결과는 비어 있었고, `git diff --check`는 오류 없이 CRLF 변환 안내만 출력했다.
- DEV-100: FightScene 1~7에서 삭제된 `EnermyAlgorithm` serialized field, missing script MonoBehaviour, GameObject component 참조를 제거했다. 현재 `EnermyAttackController` 필드인 `plateController`, `playerAttackPrediction`, `battleController` 연결은 유지했다.
- DEV-100: `enermyAlgorithm:` 및 삭제 GUID `dd2e37e0a8a3a604a802e36f2693bc28` 검색 0건, `EnermyAttackController` 필드 연결 검색, `git diff --check`를 확인했다.
- Handoff: 다음 세션은 COORD-101로 시작한다. 먼저 Battle 전체 Batch 목표와 완료 조건을 확인하고, EnemyAction/Prediction 하나에 계속 머무르지 않는다. EnemyAction은 마무리 상태만 판단하고 필수 문제가 아니면 PlayerAction 분리, TurnController 얇게 만들기, BattleController 공격 실행 책임 정리 순서로 이동한다. 씬/프리팹, 예측 확률 수치 변경, 함수명/주석만 반복 정리, 신규 공통 helper 추가, EnemyAction/Prediction에 장시간 머무는 작업은 제외한다.
