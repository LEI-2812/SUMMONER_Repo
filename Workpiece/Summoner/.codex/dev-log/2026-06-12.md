# 2026-06-12 Dev Log

## 진행 중

- DEV-061: 보호막 상태이상은 턴 만료가 아니라 피해 흡수 후 파괴 시 제거되는 계약으로 분류했다.
- `ShieldStatusEffect.StatusTurnUpdate()`가 보호막 `effectTime`을 줄이지 않도록 정리하고, `SummonFeedbackSeparationTests`에 보호막 시간 비소모 회귀 테스트를 추가했다.
- 검증은 Unity License Client 연결 거부와 `Temp/obj` 쓰기 권한 차단으로 `QA-031 Blocked`에 남겼다.
- DEV-062: 공격 방식과 상태 효과가 직관적으로 읽히도록 `TargetedAttackStrategy`의 자기 대상 효과 우회 흐름을 대상 선택 책임으로 옮기는 리팩터링 후보를 잡았다.
- DEV-062: `IAttackTargetSelector`와 Closest/Selected/All/Self 대상 선택 구현을 추가하고, Shield/OnceInvincibility의 `target = attacker` 우회를 `SelfAttackTargetSelector`로 대체했다.
- `SummonFeedbackSeparationTests`의 런타임 타입 직접 참조 오류를 파일 텍스트 기반 회귀 테스트로 바꿔 수정했다.
- 검증은 `dotnet build` 기준 `Assembly-CSharp.csproj`, `Summoner.EditModeTests.csproj` 경고 0/오류 0 통과. Unity Test Runner는 License Client 연결 거부와 readonly database 문제로 `QA-032 Blocked`에 남겼다.
- QA-031/QA-032: 새 세션 MCP EditMode `Summoner.EditModeTests.SummonFeedbackSeparationTests` 9/9 통과로 보호막 지속시간 계약과 공격 대상 선택 분리를 Pass 처리했다.
- DEV-063: 다음 기능 슬라이스는 `BattleController`와 `EnermyAlgorithm`에 남은 특수공격 대상 판정 책임 경계 검토로 선정했다.
- DEV-063 범위 확인: `EnermyAlgorithm`의 `StatusType` 분기는 AI 대응 스킬 선택 정책으로 유지하고, `BattleController.DoesAttackStrategyTargetPlayerPlate()`의 직접 상태 타입 판정만 `BenefitEffectCheck()` 계약으로 정리하는 범위로 좁혔다.
- DEV-063: `BattleController.DoesAttackStrategyTargetPlayerPlate()`를 `attackStrategy.BenefitEffectCheck()` 기준으로 정리하고, 회귀 테스트에서 BattleController의 `StatusType.Heal/Upgrade/Shield` 직접 판정을 금지했다. MCP EditMode `PlateSummonDrawStaticRegressionTests` 30/30 통과.
- DEV-064: 다음 기능 슬라이스는 `EnermyAlgorithm.ReactSpecialFrom*` 반복 분기에서 AI 정책과 실행 흐름 책임을 구분하는 검토로 선정했다.
- DEV-064 범위 확인: 상태 타입별 AI 대응 선택은 유지하고, 각 분기에서 반복하는 `attacker.IsSpecialAttackCool(...)` 검사를 `CanUseSpecialAttack` helper로 모으는 범위로 좁혔다.
- DEV-064: `EnermyAlgorithm`의 반복 쿨타임 검사를 `CanUseSpecialAttack` helper로 모으고, 상태 타입별 AI 선택 조건과 target plate 선택은 유지했다. MCP EditMode `PlateSummonDrawStaticRegressionTests` 30/30 통과.
- DEV-065: 다음 기능 슬라이스는 `EnermyAlgorithm.ApplyEnermyStatus()`의 예측용 상태 피해 계산이 실제 상태 효과 계약과 맞는지 검토하는 작업으로 선정했다.
## DEV-065 범위 확인

- CoordinatorAgent: `EnermyAlgorithm.ApplyEnermyStatus()`가 Poison/Burn/LifeDrain 예측 피해를 `GetMaxHP()` 비율로 직접 계산하는 것을 확인했다.
- DevAgent 인계: 실제 상태 효과의 `damagePerTurn` 계약을 읽는 방향으로 변경 제안을 준비한다.
- 정리 후보: Git 미추적 루트 로그 `UnityTest-DEV-061.log`, `UnityTest-DEV-062.log`만 1차 삭제 후보로 분리한다. Git 추적 파일은 별도 승인 전까지 삭제하지 않는다.

## DEV-065 완료

- DevAgent: `Summon.GetActiveStatusEffects()`를 추가해 활성 상태 효과를 읽기 전용으로 조회할 수 있게 했다.
- DevAgent: `EnermyAlgorithm.ApplyEnermyStatus()`가 Poison/Burn/LifeDrain 예측 피해를 고정 최대 HP 비율로 재계산하지 않고, 실제 `StatusEffect.damagePerTurn` 값을 사용하도록 변경했다.
- 검증: MCP EditMode `Summoner.EditModeTests.PlateSummonDrawStaticRegressionTests` 재시도 후 31/31 통과.
- 정리: Git 미추적 루트 로그 2개 삭제를 시도했으나 `UnauthorizedAccessException` 및 세션 명령 정책 제한으로 파일은 남아 있다.

## DEV-066 선정

- CoordinatorAgent: 다음 기능 슬라이스를 `EnermyAlgorithm`의 예측 상태 적용 주변 죽은 코드 정리로 선정했다.
- 근거: DEV-065에서 실제 상태 피해 계약을 `damagePerTurn`으로 맞췄지만, 같은 파일 아래에 옛 주석 구현이 `GetMaxHP()` 비율 계산을 그대로 남겨 현재 계약을 흐린다.
- DevAgent 인계: `GetLowestMonsterIndex()` 빈 미사용 메서드와 옛 주석 처리된 `GetApplyStatusEnermyPlates()`/`ApplyEnermyStatus()` 구현 제거 범위를 diff로 제시한다.

## DEV-066 완료

- DevAgent: `EnermyAlgorithm`의 빈 미사용 `GetLowestMonsterIndex()`와 예측 상태 적용 주변의 옛 주석 구현을 제거했다.
- DevAgent: 회귀 테스트가 현재 예측 상태 적용 구간을 `GetPlateController()` 전까지 확인하고, 옛 주석 구현과 빈 helper가 남지 않도록 검증하게 했다.
- 검증: 정적 검색에서 제거 대상은 테스트 금지 문자열로만 남았고, MCP EditMode `Summoner.EditModeTests.PlateSummonDrawStaticRegressionTests` 31/31 통과.

## DEV-067 선정

- CoordinatorAgent: 다음 기능 슬라이스를 `GetApplyStatusEnermyPlates()`의 Plate 참조 흐름 검토로 선정했다.
- 근거: 현재 구현은 같은 `Plate` 객체에 조정된 클론 소환수를 넣어 리스트에 추가한 뒤 즉시 원본을 복원한다. 리스트에는 Plate 참조가 들어가므로 이후 `PlayerAttackPrediction.GetPlayerAttackPredictionList()`가 조정된 상태를 보지 못할 수 있다.
- DevAgent 인계: 예측 전용 Plate/Summon 상태를 유지하는 최소 변경 범위를 제안한다.

## DEV-067 완료

- DevAgent: `GetPlayerAttackPredictionsList()`가 조정된 enemy Plate 상태를 유지한 채 `PlayerAttackPrediction.GetPlayerAttackPredictionList()`를 호출하고, `finally`에서 원본 소환수를 복원하도록 변경했다.
- DevAgent: `GetApplyStatusEnermyPlates()`는 원본 소환수 목록을 함께 채우고, 즉시 원본으로 되돌리지 않도록 정리했다.
- 검증: 정적 검색과 `git diff --check` 통과, MCP EditMode `Summoner.EditModeTests.PlateSummonDrawStaticRegressionTests` 32/32 통과.

## DEV-068 선정

- CoordinatorAgent: 다음 기능 슬라이스를 예측용 얕은 복사 계약 고정으로 정정했다.
- 근거: `Summon.Clone()`의 `MemberwiseClone()`은 예측 중 `SetNowHP()`로 복제본 HP만 조정해 원본 HP에 영향을 주지 않기 위한 의도다. 이 계약은 유지한다.
- DevAgent 인계: 얕은 복사를 교체하지 말고, 예측 상태 적용이 `DamageTake()`/`StatusTurnUpdate()`처럼 공유 참조 상태를 변경할 수 있는 경로를 쓰지 않는다는 회귀 기준을 제안한다.
