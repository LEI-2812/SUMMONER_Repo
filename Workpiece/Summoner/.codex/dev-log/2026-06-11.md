# 2026-06-11 Dev Log

## 작업 정책 변경

- 에이전트 흐름을 `CoordinatorAgent -> DevAgent -> CoordinatorAgent`로 정리하고, Review/QA/Release는 조건부 역할로 제한했다.
- 다음 작업 보고는 `ID / 도메인 / 제목 / 첫 액션 / 승인 필요 여부` 3줄로 끝내도록 기준을 세웠다.
- active 문서는 현재 판단만 남기고, 의미 있는 완료 기록은 날짜별 dev-log에 1~3줄만 남기도록 줄였다.
- `AgentModes.md`, `teams/`, `templates`, 과거 next-session note를 제거하고 에이전트 문서 구조를 최소화했다.
- 오래된 dev-log 원본과 legacy records CSV를 `.codex/archive/` 아래로 옮겨 active 기록 폴더를 줄였다.
- 기록 체계를 `records/history-summary.csv` 요약 인덱스, `dev-log` 신규 날짜 기록, `qa` 현재 QA, `archive` 원본 보관으로 정리했다.
- DevAgent가 수정 전 제안 블록과 diff 없이 `진행해`를 승인으로 처리하지 못하도록 승인 게이트를 명확히 했다.
- CoordinatorAgent는 작업 범위 확정, DevAgent는 Preflight/코드 diff/검증 기준 확정으로 역할을 분리했다.

## 기능 완료

- DEV-047: Battle/Attack 공격 전략의 쿨타임 상태를 `AttackCooldownState`로 분리했다. Unity compile warning 0, EditMode `PlateSummonDrawStaticRegressionTests` 29/29 통과.
- DEV-048: 공격 전략의 미사용 `statusTime` private 필드를 제거했다. Unity compile warning 0, EditMode `PlateSummonDrawStaticRegressionTests` 30/30 통과.
- DEV-049: 공격 전략의 미사용 public 메서드 3개를 제거했다. 호출부 없음 확인, ReviewAgent 기준 Pass, EditMode `PlateSummonDrawStaticRegressionTests` 30/30 통과.
- DEV-050: `AttackAllEnemiesStrategy.Damage` private 필드를 `damage`로 맞췄다. EditMode `PlateSummonDrawStaticRegressionTests` 30/30 통과.
- DEV-051: 공격 전략 `Attack`의 특수공격 인덱스 매개변수명을 `specialAttackArrayIndex`로 통일했다. named argument 호출 없음 확인, EditMode `PlateSummonDrawStaticRegressionTests` 30/30 통과.
- DEV-052: 공격 전략 생성자/쿨타임 상태 내부 이름을 `cooldownDuration`으로 맞추고 `GetCooltime` public 계약은 유지했다. named argument 호출 없음 확인, EditMode `PlateSummonDrawStaticRegressionTests` 30/30, `SummonDataRegressionTests` 16/16 통과.
- DEV-053: `SummonAttackData.cooltime` 직렬화 필드와 SummonData 에셋 키를 `cooldownDuration`으로 이관했다. `FormerlySerializedAs` 없이 에셋 값 보존 확인, EditMode `SummonDataRegressionTests` 16/16 통과.
- DEV-054: `SummonAttackData.GetCooltime()`을 추가하고 기존 `CooltimeGet()`은 호환 wrapper로 유지했다. Unity compile warning 0, EditMode `SummonDataRegressionTests` 17/17 통과.
- DEV-055: `SummonAttackData` public getter를 `Get` prefix 기준으로 단일화하고 중복 getter 방지 테스트를 추가했다. legacy getter 호출부 없음은 정적으로 확인했으나 Unity MCP `Transport closed`로 compile/test 검증은 QA-030으로 넘겼다.
- QA-030: DEV-055 `SummonAttackData` getter 계약 검증을 완료했다. Unity compile warning 0, EditMode `SummonDataRegressionTests` 18/18 통과.
- DEV-058: 소환수 assigned `SummonData` 적용 책임을 `Summon.TryApplyAssignedSummonData()`로 모으고, 16개 파생 소환수의 data/fallback 분기 흐름을 같은 이름으로 정리했다. Unity compile warning 0, EditMode `SummonDataRegressionTests` 23/23 통과.
- DEV-059: 다음 기능 슬라이스를 선정했다. 단순 네이밍 후보는 제외하고, `IStatusEffectTarget`이 상태 변경/피드백/알림 책임을 한 계약에 묶는 지점을 DEV-060 후보로 정했다.

## 다음 작업

- DEV-060 | Battle/Status | 상태이상 대상 계약 책임 검토.
- 첫 액션은 `Assets/Script/Battle/Status/IStatusEffectTarget.cs`와 상태이상 구현체들이 요구하는 대상 작업을 분류하고 DevAgent Preflight를 작성하는 것이다.
- 코드 변경 전 승인 필요.
