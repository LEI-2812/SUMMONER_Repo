# 2026-06-21

- DEV-136: FightScene 2~7의 `BattleCanvas` 하위 HUD 오브젝트를 1Stage 기준의 `UI_20_TurnStatus`, `UI_30_PlayerCommands`, `UI_90_ResultAlerts` 기능 그룹으로 정리했다.
- DEV-136 검증: 소환/재소환/턴종료 버튼, 턴 텍스트, 결과 알림 fileID를 유지한 채 부모 계층만 변경했다. FightScene 1~7 계층 비교와 변경한 6개 씬 대상 `git diff --check`를 통과했다.
- COORD-137: FightScene 1~7 HUD 연결 정적 검증을 통과했다. MCP AssetDatabase 씬 조회, 버튼/턴 텍스트/결과 알림 serialized reference의 기능 그룹 하위 연결, 버튼 OnClick 메서드 유지, missing script 검색, `git diff --check`를 확인했다.
- DEV-138: `PlayerAttackCondition`을 추가해 `PlayerAttackActions`의 일반공격/특수공격 사용 가능 조건과 특수공격 쿨다운 판단을 분리했다. 서브 에이전트 리뷰를 반영해 static 순수 조건 클래스로 두고 기존 호출 순서와 null 처리 의미를 유지했다.
- DEV-138 검증: 조건 private 메서드 잔존 검색 0건, `Assembly-CSharp.csproj` include 확인, 변경 파일 대상 `git diff --check` 오류 없음. `dotnet build Assembly-CSharp.csproj --no-restore`는 기존 NETSDK1004 환경 문제로 C# 컴파일 전 중단.
- COORD-139: 멀티 에이전트 2개로 DEV-138 검증을 병렬 진행했다. 공격 흐름 순서 보존과 구현 위생은 PASS였고, public API 표면 추가를 피하기 위해 `PlayerAttackCondition`을 `internal static`으로 낮춘 뒤 최종 검색, csproj include, `git diff --check`를 통과했다.
- DOC-140: Battle Batch 완료 요약을 `.codex` 문서에 정리했다. 이번 배치는 여기서 닫고 다음 구조개선은 새 목표가 있을 때 다시 범위를 잡는다.

## Battle Batch 종료 요약

- 완료 범위: FightScene 2~7 HUD 계층 정리, FightScene 1~7 HUD 연결 정적 검증, `PlayerAttackCondition` 추가, 공격 조건 책임 분리 검증.
- 검증 결과: HUD 그룹 계층 비교, 버튼 OnClick, 턴 텍스트, 결과 알림 serialized reference 유지, MCP AssetDatabase 씬 조회, 멀티 에이전트 공격 흐름 검증, `git diff --check`를 확인했다.
- 남은 환경 이슈: `dotnet build Assembly-CSharp.csproj --no-restore`는 기존 `NETSDK1004`로 C# 컴파일 전 중단된다. 원인은 `Temp\obj\Assembly-CSharp\project.assets.json` 누락이다.
- 범위 제외: `ProjectSettings/McpUnitySettings.json` 포트 변경은 이번 배치 이전부터 있던 변경으로, 현재 배치 범위에 포함하지 않았다.
- 다음 후보: 공격 후처리 분리, 특수공격 분기 정리, Turn Flow 재검토는 자동 진행하지 않고 새 목표가 있을 때 새 배치로 다시 잡는다.
- DOC-141: `.codex/workflow/architecture-role-rule.md`에 `View / Controller / Flow / Service / Action / State/Data` 역할 기준을 정리했다. 다음 후보는 새 클래스 추가가 아니라 `SummonPlaceExecutor` 같은 얇은 위임 클래스 축소로 잡았다.
- COORD-141: CoordinatorAgent 기준으로 수정 후보 큐를 `.codex/workflow/refactor-candidates.md`에 정리했다. Ready는 `SummonPlaceExecutor` 얇은 위임 축소이며, Prediction 계약 축소, Menu 중복 흐름, PlayerTargetSelection 축소 등을 Backlog로 두었다.
- COORD-141~143: `SummonPlaceExecutor` 얇은 위임 통합, `IAttackPrediction` public 계약 축소, `ToMainAlertHandler`/`ToQuitAlertHandler` 빈 override 제거를 적용했다. 빌드는 기존 NETSDK1004 환경 이슈로 C# 컴파일 전 중단됐다.
- COORD-144: `PlayerTargetSelectionActions`를 대상 선택/취소/선택 완료 콜백 책임으로 좁히고, 타겟형 특수공격 실행과 후처리를 `PlayerAttackActions`로 돌렸다. 빌드는 기존 NETSDK1004 환경 이슈로 C# 컴파일 전 중단됐다.
- COORD-145: `GameplaySettingView`의 효과 없는 `Update()` 입력 차단 시도를 제거했다. `OnlyMouse` 실제 적용은 `StoryScenarioControllerBase`가 `GameplaySettingStore.LoadOnlyMouseEnabled()`로 처리하는 경로를 유지했다. 빌드는 기존 NETSDK1004 환경 이슈로 C# 컴파일 전 중단됐다.
- COORD-146: `PlayerActionExecutor`의 공격 실행/후처리 래퍼를 제거하고 `PlayerAttackActions`가 일반 공격, 즉시 특수공격, 공격 후처리를 직접 소유하도록 정리했다. `PlayerActionExecutor`는 소환 시작과 재소환 비용 실행 역할만 남았다. 빌드는 기존 NETSDK1004 환경 이슈로 C# 컴파일 전 중단됐다.
- COORD-147: 소환수 하위 클래스 16개를 멀티 에이전트로 분담 점검했다. 고유 행동은 `Cat.SpecialAttack()` 정도지만 모든 클래스가 prefab `m_Script` GUID에 직접 연결되어 있어 자동 삭제/통합을 보류했다.
- COORD-148: `Summon.NotifyObservers()` private 축소는 `UpdateStateObserver` 인터페이스 구현 계약을 깨는 변경이라 복구했다. `Plate`와 `Summon`이 같은 observer 계약을 구현하므로 public 유지가 필요하다.
- COORD-149: `Summon`의 외부 호출 없는 setter 5개(`SetSummonName`, `SetHeavyAttackPower`, `SetMaxHP`, `SetSummonRank`, `SetImage`)를 제거했다. 재귀 검색 기준 남은 호출은 없다.
- COORD-161: 멀티 에이전트 2개로 `Summon` 런타임 `SummonType` 영향 범위를 분담 점검한 뒤, `Summon`의 `summonType` 필드/API와 fallback `SummonType` 인자를 code-only로 제거했다. Unity MCP `recompile_scripts`는 0 warning, targeted EditMode `SummonDataRegressionTests`는 실패 0건이다.
- COORD-156: `PlayerAttackCondition`의 두 조건을 `PlayerAttackActions` private 메서드로 되돌리고 별도 조건 파일을 제거했다. 검색 기준 남은 호출은 `PlayerAttackActions` 내부 5곳뿐이며, Unity MCP `recompile_scripts`는 0 warning이다.
- COORD-163: `SummonType` enum, `SummonData.summonType`, `SummonData.GetSummonType()`, 16개 `*SummonData.asset`의 `summonType:` 값을 제거했다. `Summon` 경량화 목표에 맞춰 기존 이름/등급/스탯/공격 전략 흐름은 유지했다. 검색 0건, Unity MCP `recompile_scripts` 0 warning, targeted EditMode `SummonDataRegressionTests` 실패 0건이다.
- COORD-164: `TurnSummonStateUpdater`의 턴 시작 상태효과 갱신 흐름을 `ApplyTurnStartDamageEffects`, `ApplyTurnStartControlEffects`, `UpdateNormalAttackCooldown` private 단계로 분리했다. 일반 공격 전략이 없을 때 쿨타임 갱신을 건너뛰도록 null 방어를 추가했고 scene/prefab/ScriptableObject 값은 변경하지 않았다.
- DOC-165: 리팩토링 후보와 에이전트 하네스에 완료조건 규칙을 추가했다. Ready 작업은 완료조건을 먼저 정하고, 조건을 만족하면 같은 기능의 이름/주석/미세 구조 정리로 계속 확장하지 않는다.
- COORD-154: `BattleController.isAttacking` C# 외부 직접 호출부는 없고 대표 공격 선택 상태는 `BattleAttackState`/`IsSpecialAttackTargetSelectionActive()`로 읽힌다. FightScene 1~7에 serialized `isAttacking:` 값이 남아 field 제거는 scene 영향이므로 code-only 변경 없이 보류하고 COORD-155로 넘겼다.
- COORD-155: `PlayerSummonActions`의 빈 플레이어 Plate 직접 순회를 `PlateController.GetFirstEmptyPlayerPlateIndex()` 조회로 좁혔다. `GetPlayerPlates()` 전체 제거는 공격/예측 흐름 영향이 있어 제외했다. `git diff --check`는 통과했고 `dotnet build --no-restore`는 기존 NETSDK1004 project.assets.json 누락으로 중단됐다.
- RUNTIME-WIRING-02: 멀티 에이전트 분석 결과를 바탕으로 게임 실행 차단/경고를 수습했다. 시작/이어하기의 `StageController` 의존 제거, FightScene 1~7 `PlayerController` UnityEvent 타입 복구, Thank Screen 메서드명 복구, `StorySkipView` 지연 핸들러 조회, Story 3 Fox 중복 `Animation` 제거, HUD의 잘못된 `StageController` 제거, `MenuLoader` MonoScript import 복구를 적용했다.
- RUNTIME-WIRING-02 검증: `recompile_scripts` 0 warning, 전체 EditMode 155/155 통과, 전체 PlayMode 28/28 통과, Unity Console error 0건/warning 0건, `git diff --check` 통과.
- RUNTIME-HOTFIX-03: 사용자 제보 증상 기준으로 Start Screen 설정 버튼, Story ESC, Fight 초기 결과창/일반 소환 UI를 멀티 에이전트로 분담 분석하고 수습했다. HUD 중복 로드 방지, `MenuView` 씬 잔존 방지, `MenuHandler`/`SettingHandler` 파괴 참조 방어, `BattleResultAlertView` 시작 비활성화, `SummonPickView` 일반 소환 3옵션 fallback, `Summon.GetImage()` prefab 지연 확보를 적용했다.
- RUNTIME-HOTFIX-03 검증: 신규 PlayMode 4개 추가, 전체 PlayMode 32/32 통과, 전체 EditMode 155/155 통과, Unity Console error 0건/warning 0건, `git diff --check` 통과.
