# 2026-06-23

## BATTLE-ATTACK-03

- `IBattleAttackExecution`과 `IBattleAttackTargetSelection`을 도입했다.
- `PlayerAttackUseCase`, `PlayerTargetSelectionUseCase`, `PlayerTurnUseCase`는 `BattleController` 구체 타입 대신 공격/타겟 선택 계약을 사용한다.
- `PlayerController`의 serialized `BattleController` 참조와 기존 씬 연결은 유지했다.
- 검증: RED 정적 회귀 확인, `Assets/Refresh`, `recompile_scripts` 0 warning, `PlateSummonDrawStaticRegressionTests.Player_DelegatesSpecialAttackTargetTypeToBattleController` 1/1 통과.
- 전체 `git diff --check`는 기존 `Assets/Animation/Character_Fox.controller` trailing whitespace로 실패했다. 이번 수정 범위의 `git diff --check -- <touched files>`는 통과했다.

## Next

- `TURN-ENEMY-01`: `IEnemyAttackFlow`, `EnermyAttackController`, `EnemyTurnProgressUseCase`의 적 공격 시작 실패 처리와 턴 종료 결정 계약을 먼저 Change Proposal로 제시한다.

## TURN-ENEMY-01

- `IEnemyAttackFlow.EnermyAttackStart()`가 bool을 반환하도록 바꿨다.
- `EnermyAttackController.EnermyAttackStart()`는 필수 참조 누락 시 false를 반환하고, 공격 실행 경로가 시작되면 true를 반환한다.
- `EnemyTurnProgressUseCase.RunEnemyTurn()`는 적 공격 시작 실패 시 턴 종료를 중단한다.
- 정적 회귀 테스트 기대값은 새 bool 계약과 실패 시 턴 종료 중단 흐름을 확인하도록 갱신했다.
- 검증 상태: 정적 검색과 수정 범위 `git diff --check`는 통과했다. Unity MCP `run_tests`, `recompile_scripts`, `Assets/Refresh`가 timeout으로 중단되어 Unity compile/test 결과는 미확인이다. `dotnet build .\Assembly-CSharp.csproj --no-restore`는 기존 `NETSDK1004 project.assets.json` 누락으로 중단됐다.
