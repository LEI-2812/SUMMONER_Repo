# 2026-06-07 Dev Log

## Player 특수공격 시작 흐름 정리 DEV 완료

- BACKLOG-002를 개발 완료하고 QA필요 상태로 전환했다.
- `TargetedSpecialAttackStart`는 대상 선택 경로 결정에 집중하도록 정리했다.
- 아군/적 플레이트 선택 시작은 `PlayerPlateTargetSelectionStart`, `EnermyPlateTargetSelectionStart` private 메서드로 분리했다.
- 검증: Unity compile 0 warning, `Summoner.EditModeTests.PlateSummonDrawStaticRegressionTests` 22/22 통과.
- QA 판정: 작은 private 리팩터링은 QAReviewAgent 자동 호출 대상에서 제외하도록 규칙을 변경했고, BACKLOG-002는 개발완료로 정리했다.
- records 갱신: `.codex/records/dev-records.csv`는 현재 사용자 SID에 Write Deny ACL이 있어 DEV-021 append에 실패했다.

## 에이전트 규칙 변경

- 기본 개발 흐름을 `FlowAgent -> DevAgent -> FlowAgent`로 조정했다.
- QAReviewAgent는 public API 변경, 공격 판정/예측/데이터/씬/프리팹 변경, 저장 데이터 구조 변경, 컴파일/테스트 실패, 새 테스트가 필요한 기능 변경, 명시적 QA/리뷰 요청 때만 호출한다.
- 작은 private 리팩터링, 네이밍 정리, 로그 문구 정리, 중복 제거는 DevAgent의 컴파일/관련 테스트 확인만으로 FlowAgent에 복귀한다.

## SummonController 옵션 표시 중복 정리 DEV 완료

- BACKLOG-003의 하위 작업으로 `SummonController` 일반 소환/재소환 옵션 표시 중복을 정리했다.
- `OpenDrawOptions`와 `OpenRedrawOptions`의 옵션 패널 채우기 반복을 `ShowDrawOptions` private 메서드로 모았다.
- 제외: 공격 예측 계열 네이밍, 넓은 getter/setter 네이밍 변경, 씬/프리팹 변경.
- 검증: Unity compile 0 warning, `Summoner.EditModeTests.PlateSummonDrawStaticRegressionTests` 22/22 통과.
- QA 판정: public API와 런타임 의미 변경이 없는 작은 private 리팩터링이므로 QAReviewAgent 호출을 생략했다.
- records 갱신: `.codex/records/dev-records.csv`는 현재 사용자 SID에 Write Deny ACL이 있어 DEV append를 생략했다.

## SummonController 재소환 플레이트 인덱스 조회 정리 DEV 완료

- BACKLOG-004의 하위 작업으로 `SummonController.SelectPlate`의 플레이어 플레이트 인덱스 조회를 정리했다.
- 직접 리스트를 순회하던 코드를 기존 `PlateController.GetPlayerPlateIndex` 호출로 바꿨다.
- 선택한 플레이트가 플레이어 플레이트가 아니면 로그를 남기고 재소환 옵션 선택을 시작하지 않도록 방어했다.
- 제외: 새 `PlateSelectionController` 생성, 씬/프리팹 연결 변경, 재소환 UX 변경.
- 검증: Unity compile 0 warning, `Summoner.EditModeTests.PlateSummonDrawStaticRegressionTests` 22/22 통과.
- QA 판정: public API와 런타임 의미 변경이 없는 작은 private 리팩터링이므로 QAReviewAgent 호출을 생략했다.
- records 갱신: `.codex/records/dev-records.csv`는 현재 사용자 SID에 Write Deny ACL이 있어 DEV append를 생략했다.

## 자동 루프 종료

- 자동 루프에서 안전하게 처리 가능한 작은 하위 작업 2개를 완료했다.
- 남은 BACKLOG-003 네이밍 정리는 호출부가 넓어 별도 범위 선정이 필요하다.
- 남은 BACKLOG-004 새 컨트롤러 분리는 새 파일/구조 변경 가능성이 있어 자동 진행을 보류했다.

## DrawOptionPanelView 소환수 배정 네이밍 정리 DEV 완료

- BACKLOG-003의 하위 작업으로 `DrawOptionPanelView.setAssignedSummon` 네이밍을 호환 유지 방식으로 정리했다.
- `SetAssignedSummon`을 추가하고 기존 `setAssignedSummon`은 wrapper로 유지했다.
- `SummonController`의 옵션 패널 배정 호출부는 새 이름을 사용하도록 바꿨다.
- 검증: Unity compile 0 warning, `Summoner.EditModeTests.PlateSummonDrawStaticRegressionTests` 22/22 통과.
- QA 판정: 기존 wrapper를 유지한 작은 네이밍 정리이므로 QAReviewAgent 호출을 생략했다.

## SummonController 재소환 빈 플레이트 확인 정리 DEV 완료

- BACKLOG-004의 하위 작업으로 `SummonController.StartRedraw`의 빈 플레이어 플레이트 확인 흐름을 정리했다.
- 직접 `GetPlayerPlates()[0..2]`를 확인하던 조건을 기존 `PlateController.IsPlayerPlateClear()` 호출로 바꿨다.
- 제외: 새 `PlateSelectionController` 생성, 씬/프리팹 연결 변경, 재소환 UX 변경.
- 검증: Unity compile 0 warning, `Summoner.EditModeTests.PlateSummonDrawStaticRegressionTests` 22/22 통과.
- QA 판정: public API와 런타임 의미 변경이 없는 작은 리팩터링이므로 QAReviewAgent 호출을 생략했다.

## SummonController 플레이트 인덱스 조회 위임 정리 DEV 완료

- BACKLOG-004의 하위 작업으로 `SummonController.GetPlayerPlateIndex`와 `GetEnermyPlateIndex`의 내부 구현을 정리했다.
- 기존 public 메서드는 유지하고, 내부에서 `PlateController.GetPlayerPlateIndex`와 `GetEnermyPlateIndex`로 위임하도록 바꿨다.
- 제외: 새 `PlateSelectionController` 생성, 씬/프리팹 연결 변경, public 호출 계약 변경.
- 검증: Unity compile 0 warning, `Summoner.EditModeTests.PlateSummonDrawStaticRegressionTests` 22/22 통과.
- QA 판정: public API와 런타임 의미 변경이 없는 작은 리팩터링이므로 QAReviewAgent 호출을 생략했다.

## DrawOptionPanelView 소환수 조회 네이밍 정리 DEV 완료

- BACKLOG-003의 하위 작업으로 `DrawOptionPanelView.getAssignedSummon` 네이밍을 호환 유지 방식으로 정리했다.
- `GetAssignedSummon`을 추가하고 기존 `getAssignedSummon`은 wrapper로 유지했다.
- 검증: Unity compile 0 warning, `Summoner.EditModeTests.PlateSummonDrawStaticRegressionTests` 22/22 통과.
- QA 판정: 기존 wrapper를 유지한 작은 네이밍 정리이므로 QAReviewAgent 호출을 생략했다.

## DrawOptionPanelView 소문자 wrapper 제거 DEV 완료

- `DrawOptionPanelView`의 `getAssignedSummon`과 `setAssignedSummon` wrapper를 삭제했다.
- `GetAssignedSummon`과 `SetAssignedSummon`만 남겨 함수명을 대문자 시작으로 통일했다.
- 검증: Unity compile 0 warning, `Summoner.EditModeTests.PlateSummonDrawStaticRegressionTests` 22/22 통과.
- QA 판정: 작은 네이밍 정리이므로 QAReviewAgent 호출을 생략했다.

## Summon 이름 조회 네이밍 정리 DEV 완료

- BACKLOG-003의 하위 작업으로 `Summon.getSummonName()`을 `GetSummonName()`으로 통일했다.
- 같은 기능을 하는 소문자 wrapper는 남기지 않았다.
- `Assets` 아래 C# 호출부 전체를 함께 변경했다.
- 검증: 대소문자 구분 검색 기준 `getSummonName(` 0건, `GetSummonName(` 47건.
- 검증: Unity compile 0 warning, `Summoner.EditModeTests.PlateSummonDrawStaticRegressionTests` 22/22 통과.

## Summon 주요 getter/setter 네이밍 정리 DEV 완료

- BACKLOG-003의 하위 작업으로 `Summon.getSummonRank()`를 `GetSummonRank()`로 통일했다.
- `Summon.getImage()`를 `GetImage()`로 통일했다.
- 호출부가 없던 `setSummonName`, `setSummonRank` 선언을 `SetSummonName`, `SetSummonRank`로 변경했다.
- 같은 기능을 하는 소문자 wrapper는 남기지 않았다.
- 검증: 대상 소문자 getter/setter 검색 결과 0건.
- 검증: Unity compile 0 warning, `Summoner.EditModeTests.PlateSummonDrawStaticRegressionTests` 22/22 통과.

## 작업 정책 변경

- `AGENTS.md`를 프로젝트 원칙, 문서 읽기 정책, 승인 규칙 중심으로 압축했다.
- `.agents/README.md`에 빠른 라우팅 표를 추가해 요청별로 필요한 문서만 읽도록 정리했다.
- `.agents/AgentWorkflow.md`는 전체 흐름, 품질 게이트, 상태 기준만 남기고 상세 산출물 중복을 줄였다.
- `FlowAgent`, `DevAgent`, `QAReviewAgent` role 문서는 필수 확인과 결과 기록 중심으로 압축했다.
- `.codex/workflow/automation-rule.md`는 자동 루프 예외 기준만 남기고 상세 역할 기준은 현재 `Ready` role 문서를 따르도록 정리했다.

## 기록 문서 정리

- `.codex/dev-log/README.md`를 추가해 날짜별 기록 형식을 짧은 기준으로 다시 정의했다.
- 긴 `WorkLogTemplate.md`는 실제 운영과 맞지 않아 제거했다.
- 사용하지 않는 `.codex/code-review/active-review.md`를 제거하고, 코드 리뷰는 QAReviewAgent 결과에 포함하도록 정리했다.
- 예전 세션 인계 방식인 `archive/session-guide.md`와 예전 변경 요약인 `archive/change-summary.md`를 제거했다.
- 과거 workflow-history의 핵심 완료 요약은 `2026-06-06.md`에 흡수하고, 별도 workflow-history 파일은 제거했다.

## 완료 이력 CSV 관리 도입

- `.codex/records/dev-records.csv`와 `.codex/records/qa-records.csv`를 추가했다.
- `issue-board.md`의 완료 요약은 records CSV로 이관하고, issue-board는 현재 이슈와 Backlog 중심으로 줄였다.
- FlowAgent는 DEV 완료 시 `dev-records.csv`, QAReviewAgent는 QA 완료 시 `qa-records.csv`에 한 줄을 추가하도록 기준을 정리했다.
