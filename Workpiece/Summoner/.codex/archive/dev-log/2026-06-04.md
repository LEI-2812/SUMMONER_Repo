# Work Log - 2026-06-04

## 에이전트 운영 문서 최소화

### 진행 내용

- QAAgent 역할을 유지하되 Smoke Check 담당으로 축소했다.
- Auto QA 중심 흐름을 제거하고 DevAgent 중심 개발 흐름으로 정리했다.
- 긴 QA feature 절차 문서를 삭제했다.
- `active-qa.md`를 짧은 Smoke Board로 교체했다.
- `regression-checklist.md`를 최소 회귀 확인 체크리스트로 교체했다.
- workflow 문서를 현재 개발 진행 기준으로 다시 작성했다.
- 깨진 과거 code-review 문서를 현재 리뷰 없음 상태로 정리했다.
- `AgentDocs/reports/AUTO_QA_REPORT.md`를 삭제했다.

### 삭제한 문서

- `.codex/qa/features/BattleContent.md`
- `.codex/qa/features/BattleCore.md`
- `.codex/qa/features/GameSystem.md`
- `.codex/qa/features/OptionSystem.md`
- `.codex/qa/features/SceneObject.md`
- `.codex/qa/features/StorySystem.md`
- `AgentDocs/reports/AUTO_QA_REPORT.md`

### 남긴 QA 기준

- QAAgent는 깊은 QA 문서를 만들지 않는다.
- QAAgent는 `Pass`, `Needs Fix`, `Blocked` 중 하나로 짧게 판단한다.
- MCP timeout, 501, WebSocket error는 게임 코드 실패와 분리한다.
- 같은 MCP 오류는 2회까지만 재시도한다.
- PlayMode가 막히면 개발 진척을 멈추지 않고 `Blocked`로 둔다.

### 변경하지 않은 것

- 게임 코드
- 테스트 코드
- 씬
- 프리팹
- 에셋
- 저장 데이터 구조

### 검증

- `git diff --check` 통과. 줄바꿈 CRLF 경고만 있음.

## MCP PlayMode 연결 유지 수정

### 진행 내용

- PlayMode 진입 시 MCP 서버가 끊기는 원인을 확인했다.
- 원인은 `McpUnityServer.OnPlayModeStateChanged`에서 `ExitingEditMode` 상태에 서버를 직접 중지하는 코드였다.
- 이 코드는 최근 사용자 수정이 아니라 MCP Unity 패키지 도입 당시부터 있던 동작으로 확인했다.
- 사용자는 Unity Editor를 항상 켜 둔 상태에서 QA를 진행하므로, PlayMode 진입 시 MCP 서버를 끄지 않도록 수정했다.
- PlayMode 진입 중 서버가 이미 내려간 경우를 대비해 `EnteredPlayMode`에서 Auto Start 설정이 켜져 있으면 다시 시작하도록 보강했다.

### 변경한 파일

- `LocalPackages/com.gamelovers.mcp-unity/Editor/UnityBridge/McpUnityServer.cs`

### 검증

- MCP `recompile_scripts`: 0 warning 통과
- PlayMode 단일 테스트 `GameSaveContinueTests.ContinueButton_Hides_WhenSaveMissing`: 1/1 통과
- PlayMode 단일 테스트 `GameSaveProgressReadTests.GetGameSaveOrDefault_LoadsPlayerPrefs_WhenControllerIsMissing`: 1/1 통과
- PlayMode 단일 테스트 `StageRuntimeFlowPlayModeTests.StartScreen_LoadsSaveAndStageFlowControllers`: 1/1 통과
- PlayMode 전체 테스트: 25/25 통과
- EditMode 전체 테스트: 89/89 통과
- Console error/warning: 없음

## QA 재진행

### 진행 내용

- MCP 연결 수정 후 기존 Active QA 항목을 다시 확인했다.
- QA-003은 PlayMode 중 MCP 연결 유지와 전체 PlayMode 테스트 통과로 Pass 처리했다.
- QA-008은 저장 진행도와 스테이지 흐름 PlayMode 테스트 통과로 Pass 처리했다.
- QA-002, QA-004, QA-005, QA-007, QA-010은 전체 QA Smoke에서 연결과 테스트 범위 기준으로 Pass 처리했다.
- QA-012는 Console warning 없음과 MCP WebSocket error 분리 기준으로 Pass 유지했다.

### 변경한 문서

- `.codex/qa/active-qa.md`
- `.codex/workflow/issue-board.md`

### 결과

- 현재 Active QA 항목은 모두 Pass다.
- PlayMode 중 MCP 연결이 끊기던 문제는 현재 검증 기준으로 해결됐다.

## 다음 개발 항목 분석

### 확인한 내용

- 플레이어 6종 `Cat`, `Rabbit`, `Snake`, `Wolf`, `Eagle`, `Fox`는 이미 `SummonData` 에셋과 프리팹 연결이 있다.
- 적 소환수 `Slime`, `Skeleton`, `LowDevil`, `HighDevil`, `KingSlime`, `QueenSpirit`, `FireSpirit`, `WaterSpirit`, `GrassSpirit`, `DarkDragon`은 아직 대부분 `Awake()`에서 수치와 공격 전략을 직접 세팅한다.
- 적 소환수 프리팹에는 `summonData` 연결이 없다.
- `BattleController`와 `EnermyAlgorithm`은 공격 전략 타입과 상태 타입을 직접 분기하므로, 전투 시스템 리팩터링 전에 Summon 데이터 입력을 먼저 안정화하는 것이 안전하다.

### 다음 세션 작업으로 기록한 내용

- `DEV-001`: Summon 데이터화 2단계
- 첫 범위는 `Slime`, `Skeleton` 2종만 진행한다.
- `SummonType` enum 뒤쪽에 적 타입을 추가한다.
- `SlimeSummonData.asset`, `SkeletonSummonData.asset`를 만들고 각 프리팹에 연결한다.
- 기존 하드코딩 수치와 공격 전략이 유지되는지 `SummonDataRegressionTests`에 회귀 테스트를 추가한다.

### 변경한 문서

- `.codex/workflow/current-task.md`
- `.codex/workflow/issue-board.md`
- `.codex/workflow/agent-status.md`
