# Dev Log - 2026-06-08

## BACKLOG-004 PlateSelectionController 분리 확인

- `SummonController`의 재소환 플레이트 선택 시작/조회 흐름이 `PlateSelectionController`로 분리된 상태를 확인했다.
- `SummonController`는 소환 옵션 표시와 선택 진행 흐름을 유지하고, 재소환 가능한 플레이트 확인과 플레이어 플레이트 인덱스 조회는 `PlateSelectionController`가 맡는다.
- `Assets/getParentSummonImage.cs`는 기존 GUID를 유지한 채 `Assets/ParentSummonImage.cs`와 `ParentSummonImage` 클래스명으로 정리된 상태를 확인했다.

## 검증

- Unity script recompile: 0 warning
- EditMode `Summoner.EditModeTests.PlateSummonDrawStaticRegressionTests`: 23/23 passed

## 다음

- FlowAgent가 비메서드 네이밍 잔여 항목 또는 다음 유지보수 백로그를 새 기능 목표로 선정한다.

## DEV-022 FireSpirit SummonData 적용 DEV 완료

- `FireSpirit`가 `SummonDataApply(SummonDataGet())`를 먼저 시도하고, 데이터가 없으면 기존 fallback 값을 유지하도록 정리했다.
- `SummonType.FireSpirit`는 기존 enum 직렬화 값을 흔들지 않도록 마지막에 추가했다.
- `FireSpiritSummonData.asset`을 추가하고 `FireSpirit.prefab`의 `summonData` 참조를 연결했다.
- TDD RED: `SummonDataRegressionTests`에서 FireSpirit enum, 에셋, prefab 참조 누락으로 4개 실패를 확인했다.
- 검증: Unity compile 0 warning, EditMode `Summoner.EditModeTests.SummonDataRegressionTests` 8/8 통과.

## QA-022 FireSpirit SummonData 적용 QA 완료

- `FireSpiritSummonData.asset` 값이 기존 fallback 수치와 공격 전략을 유지하는지 확인했다.
- `FireSpirit.prefab`의 `summonData`가 새 데이터 에셋 GUID를 참조하는지 확인했다.
- QA 검증: Unity compile 0 warning, EditMode `Summoner.EditModeTests.SummonDataRegressionTests` 8/8 통과.

## DEV-023 QueenSpirit SummonData 적용 DEV 완료

- `QueenSpirit`가 `SummonDataApply(SummonDataGet())`를 먼저 시도하고, 데이터가 없으면 기존 fallback 값을 유지하도록 정리했다.
- `SummonType.QueenSpirit`는 기존 enum 직렬화 값을 흔들지 않도록 마지막에 추가했다.
- `QueenSpiritSummonData.asset`을 추가하고 `QueenSpirit.prefab`의 `summonData` 참조를 연결했다.
- `SummonDataRegressionTests`가 3개 특수공격 데이터도 검증할 수 있게 테스트 보조 로직을 확장했다.
- TDD RED: `SummonDataRegressionTests`에서 QueenSpirit enum, 에셋, prefab 참조 누락으로 4개 실패를 확인했다.
- 검증: Unity compile 0 warning, EditMode `Summoner.EditModeTests.SummonDataRegressionTests` 8/8 통과.

## QA-023 QueenSpirit SummonData 적용 QA 완료

- `QueenSpiritSummonData.asset` 값이 기존 fallback 수치와 3개 특수공격 전략을 유지하는지 확인했다.
- `QueenSpirit.prefab`의 `summonData`가 새 데이터 에셋 GUID를 참조하는지 확인했다.
- QA 검증: Unity compile 0 warning, EditMode `Summoner.EditModeTests.SummonDataRegressionTests` 8/8 통과.

## DEV-024 DarkDragon SummonData 적용 DEV 완료

- `DarkDragon`이 `SummonDataApply(SummonDataGet())`를 먼저 시도하고, 데이터가 없으면 기존 fallback 값을 유지하도록 정리했다.
- `SummonType.DarkDragon`은 기존 enum 직렬화 값을 흔들지 않도록 마지막에 추가했다.
- `DarkDragonSummonData.asset`을 추가하고 `DarkDragon.prefab`의 `summonData` 참조를 연결했다.
- `SummonDataRegressionTests`가 4개 특수공격 데이터도 검증할 수 있게 테스트 보조 로직을 확장했다.
- TDD RED: `SummonDataRegressionTests`에서 DarkDragon enum, 에셋, prefab 참조 누락으로 4개 실패를 확인했다.
- 검증: Unity compile 0 warning, EditMode `Summoner.EditModeTests.SummonDataRegressionTests` 8/8 통과.

## QA-024 DarkDragon SummonData 적용 QA 완료

- `DarkDragonSummonData.asset` 값이 기존 fallback 수치와 4개 특수공격 전략을 유지하는지 확인했다.
- `DarkDragon.prefab`의 `summonData`가 새 데이터 에셋 GUID를 참조하는지 확인했다.
- QA 검증: Unity compile 0 warning, EditMode `Summoner.EditModeTests.SummonDataRegressionTests` 8/8 통과.

## DEV-025 전투 공격 실행 흐름 분리 분석 DEV 완료

- `BattleController`의 특수공격 대상 plate 선택 책임을 `SpecialAttackTargetPlatesGet` private 메서드로 분리했다.
- `Targeted`, `AllEnemies`, `ClosestEnemy` 특수공격 실행 메서드는 대상 plate를 직접 고르지 않고 공통 helper 결과를 사용한다.
- public API, 공격 전략, 씬, 프리팹, 저장 데이터 구조는 변경하지 않았다.
- TDD RED: `BattleController_UsesSharedSpecialAttackTargetPlateResolution`에서 helper 부재로 실패를 확인했다.
- 검증: Unity compile 0 warning, EditMode `Summoner.EditModeTests.PlateSummonDrawStaticRegressionTests` 24/24 통과.

## QA-025 전투 공격 실행 흐름 분리 분석 QA 완료

- `SpecialAttackTargetPlatesGet`의 플레이어/적, 이로운/해로운 효과 대상 매핑이 기존 분기와 같은지 확인했다.
- `SpecialAttackExecute` public API와 `Summon.SpecialAttack` 호출 계약은 변경하지 않았다.
- QA 검증: Unity compile 0 warning, EditMode `Summoner.EditModeTests.PlateSummonDrawStaticRegressionTests` 24/24 통과.

## DEV-026 적 반응 일반공격 실행 중복 분리 DEV 완료

- `EnermyAlgorithm.HandleReactNormalAttack`의 적 일반공격 실행 중복을 `EnemyNormalAttackExecute` private 메서드로 분리했다.
- 30% 강공격의 공격력 교체, 가장 가까운 플레이어 plate 공격, 공격력 원복 흐름은 `EnemyHeavyNormalAttackExecute`에 모았다.
- public API, 예측 알고리즘, 공격 확률, 씬, 프리팹, 저장 데이터 구조는 변경하지 않았다.
- TDD RED: `EnermyAlgorithm_UsesSharedNormalAttackExecution`에서 helper 부재로 실패를 확인했다.
- 검증: Unity compile 0 warning, EditMode `Summoner.EditModeTests.PlateSummonDrawStaticRegressionTests` 25/25 통과.

## QA-026 적 반응 일반공격 실행 중복 분리 QA 완료

- `EnemyNormalAttackExecute`가 기존처럼 30% 강공격 또는 targetPlateIndex 일반공격을 선택하는지 확인했다.
- `EnemyHeavyNormalAttackExecute`가 공격력을 강공격력으로 바꾼 뒤 가장 가까운 플레이어 plate를 공격하고 원래 공격력으로 되돌리는지 확인했다.
- QA 검증: Unity compile 0 warning, EditMode `Summoner.EditModeTests.PlateSummonDrawStaticRegressionTests` 25/25 통과.

## DEV-027 적 반응 특수공격 실행 로그 중복 분리 DEV 완료

- `EnermyAlgorithm`의 특수공격 실행과 로그 출력 반복을 `EnemySpecialAttackExecute` private 메서드로 분리했다.
- 특수공격 대상 plate index, 특수공격 배열 index, 로그 문구는 기존 값을 그대로 helper에 전달한다.
- public API, 예측 알고리즘, 공격 확률, 씬, 프리팹, 저장 데이터 구조는 변경하지 않았다.
- TDD RED: `EnermyAlgorithm_UsesSharedSpecialAttackExecutionLog`에서 helper 부재로 실패를 확인했다.
- 검증: Unity compile 0 warning, EditMode `Summoner.EditModeTests.PlateSummonDrawStaticRegressionTests` 26/26 통과.

## QA-027 적 반응 특수공격 실행 로그 중복 분리 QA 완료

- `EnemySpecialAttackExecute`가 기존 `battleController.SpecialAttackExecute` 인자와 로그 문구를 그대로 받는지 확인했다.
- `EnermyAlgorithm` 내부 직접 특수공격 실행 호출이 helper 안으로 모였는지 확인했다.
- QA 검증: Unity compile 0 warning, EditMode `Summoner.EditModeTests.PlateSummonDrawStaticRegressionTests` 26/26 통과.

## 도메인 유지보수 연속성 기준 추가

- 사용자가 `Summons` 도메인 잔여 정리가 끝나기 전에 `Battle` 도메인으로 넘어간 문제를 지적했다.
- `AGENTS.md`, `.agents/roles/FlowAgent.md`, `.codex/workflow/automation-rule.md`에 같은 도메인의 잔여 유지보수 후보를 먼저 확인하는 기준을 추가했다.
- 다음 작업은 `Battle` 후속 리팩터링이 아니라 DEV-028 `Summons` 도메인 잔여 구조 정리로 되돌렸다.

## DEV-028A 소환수 단순 override 제거

- `Cat`은 고유 `SpecialAttack`/`ApplayMultiple` 동작 때문에 override 유지 대상으로 분류했다.
- 플레이어 소환수 `Rabbit`, `Snake`, `Wolf`, `Eagle`, `Fox`의 단순 `Die`/`TakeDamage` pass-through override를 제거했다.
- 적/스피릿 소환수 `Slime`, `Skeleton`, `LowDevil`, `HighDevil`, `KingSlime`, `WaterSpirit`, `GrassSpirit`, `FireSpirit`, `QueenSpirit`, `DarkDragon`의 단순 `Die`/`TakeDamage` pass-through override를 제거했다.
- public 상속 동작은 `Summon`의 기존 `Die`/`TakeDamage`를 그대로 사용하므로 런타임 의미 변경 없이 개별 클래스 책임을 줄였다.
- TDD RED: 플레이어 소환수와 적/스피릿 소환수 override 잔여를 잡는 정적 회귀 테스트가 제거 전 실패하는 것을 확인했다.
- 검증: Unity compile 0 warning, EditMode `Summoner.EditModeTests.SummonDataRegressionTests` 10/10 통과.

## 다음

- 같은 `Summons` 도메인에서 `Awake()`와 `SummonInitialize()` 초기화 차이를 분류한다.
- `SummonData` 값과 fallback 값 일치 기준 중 테스트가 부족한 지점을 다음 작은 후보로 선정한다.

## DEV-028B 플레이어 소환 후보 초기화 정리

- `SummonController.Awake()`에서 소환 후보 목록을 `DrawSummonsInitialize`로 한 번 초기화하도록 정리했다.
- `Rabbit`, `Snake`에만 있던 `Start()` 기반 `SummonInitialize()` 호출을 제거했다.
- 등급별 후보 선택은 `SummonController`가 담당하므로, 후보 rank를 사용할 준비도 컨트롤러 초기화 단계에서 처리하게 했다.
- 개별 플레이어 소환수 클래스는 자기 수치와 고유 공격 동작만 유지하고, 소환 후보 목록 초기화 책임은 갖지 않는다.
- TDD RED: `DrawSummonsInitialize` 부재와 `Rabbit`의 `Start()` 초기화 잔여로 실패하는 것을 확인했다.
- 검증: Unity compile 0 warning, EditMode `Summoner.EditModeTests.SummonDataRegressionTests` 12/12 통과.

## 다음

- 플레이어 소환수 assigned data 검증이 `Cat`에만 머무는지 확인한다.
- 필요한 경우 `Rabbit`, `Snake`, `Wolf`, `Eagle`, `Fox`까지 assigned data 회귀 테스트를 확장한다.

## DEV-028C 플레이어 assigned SummonData 검증 보강

- 플레이어 소환수 assigned data 적용 검증을 `Cat` 단일 테스트에서 `Cat`, `Rabbit`, `Snake`, `Wolf`, `Eagle`, `Fox` 6종 전체로 확장했다.
- 테스트용 `SummonData` 생성 메서드를 플레이어 6종 기준으로 추가했다.
- 실제 production 코드는 변경하지 않고, 데이터 적용 경로의 회귀 감지만 강화했다.
- TDD RED: 플레이어 assigned data 테스트 데이터가 `Cat`에만 있어 `Rabbit` 커버리지 부재로 실패하는 것을 확인했다.
- 검증: Unity compile 0 warning, EditMode `Summoner.EditModeTests.SummonDataRegressionTests` 14/14 통과.

## 다음

- 같은 `Summons` 도메인에서 남은 `Start()` 로그성 초기화와 fallback/data 불일치 후보를 확인한다.
- public API나 런타임 의미 변경 후보가 나오면 QAReviewAgent 전환 여부를 먼저 기록한다.

## DEV-028D 로그성 Start 제거

- `LowDevil`, `HighDevil`에 남아 있던 체력 로그 전용 `Start()`를 제거했다.
- 해당 클래스의 데이터/fallback 초기화는 기존 `Awake()` 흐름에 그대로 유지했다.
- TDD RED: `LowDevil`의 로그성 `Start()` 잔여로 실패하는 것을 확인했다.
- 검증: Unity compile 0 warning, EditMode `Summoner.EditModeTests.SummonDataRegressionTests` 15/15 통과.

## 다음

- FlowAgent가 `Summons` 도메인 잔여 후보를 최종 분류한다.
- public API나 런타임 의미 변경 후보가 없으면 DEV-028을 정리하고 다음 도메인으로 넘어갈 수 있다.

## QA-028 소환수 도메인 잔여 구조 정리 QA 완료

- `SummonController.DrawSummonsInitialize`가 플레이어 후보 목록 rank 준비를 컨트롤러 책임으로 모으는지 확인했다.
- 플레이어 소환수 6종과 적/스피릿 소환수 데이터 적용 회귀 테스트가 모두 통과했다.
- `Summons` 하위에 `Start()`/단순 `Die`/단순 `TakeDamage`/직접 `SummonInitialize()` 호출 잔여가 없는지 정적 확인했다.
- QA 검증: Unity compile 0 warning, EditMode `Summoner.EditModeTests.SummonDataRegressionTests` 15/15, `Summoner.EditModeTests.PlateSummonDrawStaticRegressionTests` 26/26 통과.

## DEV-028 완료 정리

- 소환수 도메인 잔여 구조 정리는 QA-028 Pass로 닫는다.
- 새 소환수 기능 요구가 나오기 전까지 다음 유지보수 후보는 FlowAgent가 다른 도메인에서 선정할 수 있다.

## DEV-028E 소환 후보 rank 조회 책임 재정정

- 사용자가 `SummonController`에서 소환수를 초기화하는 책임 위치를 지적했다.
- `SummonController.DrawSummonsInitialize`를 제거해 후보 선택 단계에서 `SummonInitialize()`를 호출하지 않게 했다.
- `Summon.GetDrawRank()`를 추가해 소환 후보 등급 선택은 `SummonData`의 rank를 먼저 읽고, 데이터가 없을 때만 기존 `summonRank` fallback을 사용하게 했다.
- `SummonController.SelectRandomSummonByRank`는 `GetSummonRank()` 대신 `GetDrawRank()`로 후보를 필터링한다.
- TDD RED: `SummonController`가 여전히 `SummonInitialize()`를 호출하고 `GetSummonRank()`로 필터링해 실패하는 것을 확인했다.
- 검증: Unity compile 0 warning, EditMode `Summoner.EditModeTests.SummonDataRegressionTests` 16/16, `Summoner.EditModeTests.PlateSummonDrawStaticRegressionTests` 26/26 통과.

## QA-028 재검증

- 소환 후보 선택은 초기화된 mutable 상태가 아니라 `SummonData`의 rank를 기준으로 동작하도록 재검증했다.
- 실제 소환 초기화는 기존처럼 `Plate.SummonPlaceOnPlate()`에서 `SummonInitialize()`가 호출되는 흐름을 유지한다.
- QA 재검증: Unity compile 0 warning, EditMode `Summoner.EditModeTests.SummonDataRegressionTests` 16/16, `Summoner.EditModeTests.PlateSummonDrawStaticRegressionTests` 26/26 통과.

## DEV-028F 플레이어 소환수 배수 중복 정리

- 사용자가 아직 `Summons` 도메인이 끝나지 않았다고 지적해 Battle 도메인 이동을 중단했다.
- `Cat`, `Rabbit`, `Wolf`, `Eagle`, `Fox`의 중복 `ApplayMultiple` override를 제거했다.
- `Snake.ApplyMultiple` 별도 구현을 제거하고 base `ApplayMultiple(multiple)` 호출로 맞췄다.
- 플레이어 소환수 fallback 배수 적용 책임을 `Summon`에 모았고, 개별 소환수 클래스는 고유 수치와 고유 공격 동작만 유지한다.
- 검증: Unity compile 0 warning, EditMode `Summoner.EditModeTests.SummonDataRegressionTests.PlayerSummons_KeepCurrentValuesBeforeDataMigration` 1/1 통과.

## 다음

- 같은 `Summons` 도메인에서 `Summon.cs` 본체의 public 필드, 상태/뷰/데이터 책임 혼합 후보를 분류한다.
- Battle/예측/공격 로직으로 이동하지 않는다.

## DEV-028G Summon 상태 필드 공개 범위 정리

- `Summon.attackPower`, `Summon.heavyAttakPower`, `Summon.nowHP`, `Summon.isAttack`의 공개 범위를 `[SerializeField] protected`로 낮췄다.
- Unity 직렬화 필드명은 유지하고, 외부 직접 변경은 getter/setter와 상태 타깃 메서드로만 하게 했다.
- 외부 직접 필드 접근이 없는지 정적 확인했다.
- 검증: Unity compile 0 warning.

## 다음

- UI가 직접 읽는 `normalAttackSprite`, `specialAttackSprite` 필드를 getter로 감쌀 수 있는지 확인한다.
- `Summon.cs` 본체의 상태/뷰/데이터 책임 혼합 후보를 계속 분류한다.

## DEV-028H 공격 스프라이트 필드 공개 범위 정리

- `Summon.normalAttackSprite`, `Summon.specialAttackSprite`의 공개 범위를 `[SerializeField] private`로 낮췄다.
- `SummonStatePanelView`는 필드를 직접 읽지 않고 `GetNormalAttackSprite()`, `GetSpecialAttackSprite()`를 통해 버튼 스프라이트를 설정한다.
- Unity 직렬화 필드명은 유지하고, UI는 소환수 내부 필드 저장 방식에 직접 의존하지 않게 했다.
- 검증: Unity compile 0 warning.

## DEV-028I 소환수 스탯 배수 전역 필드 캡슐화

- `Summon.multiple`의 공개 범위를 private static으로 낮췄다.
- 플레이어 소환수 fallback 초기화는 `StatMultiplierGet()`으로 현재 배수를 읽고, Stage 쪽은 `StatMultiplierSet(value)`로 배수를 설정하게 했다.
- Stage는 소환수 전역 필드 이름과 보관 방식에 직접 의존하지 않고, 소환수 배수 설정 요청만 하도록 책임을 줄였다.
- 검증: Unity compile 0 warning, EditMode `Summoner.EditModeTests.SummonDataRegressionTests.PlayerSummons_KeepCurrentValuesBeforeDataMigration` 1/1 통과.

## 다음

- 같은 `Summons` 도메인에서 `Summon.cs` 본체의 상태/뷰/데이터 책임 혼합 후보를 계속 분류한다.
- Battle/예측/공격 로직으로 이동하지 않는다.

## DEV-028J Summon 뷰/애니메이션 필드 공개 범위 정리

- `Summon.shieldImage`, `Summon.animator`의 공개 범위를 `[SerializeField] private`로 낮췄다.
- 파생 소환수 클래스에서 두 필드를 직접 사용하지 않는지 정적 확인했다.
- 쉴드 표시와 공격/피격 애니메이션 트리거는 계속 `Summon` 본체 흐름에서만 처리한다.
- 검증: Unity compile 0 warning.

## 다음

- 같은 `Summons` 도메인에서 파생 클래스 fallback 초기화가 직접 만지는 상태 필드 중 추가로 책임을 줄일 수 있는 후보를 분류한다.
- Battle/예측/공격 로직으로 이동하지 않는다.

## DEV-028K Summon 보호막/무적 상태 필드 공개 범위 정리

- `Summon.shield`, `Summon.onceInvincibility`의 공개 범위를 private으로 낮췄다.
- 외부 상태이상 처리는 기존처럼 `ShieldAdd`, `ShieldSet`, `OnceInvincibilitySet` 메서드와 `IStatusEffectTarget` 경로를 사용한다.
- 파생 소환수 클래스에서 두 상태 필드를 직접 사용하지 않는지 정적 확인했다.
- 검증: Unity compile 0 warning.

## 다음

- 같은 `Summons` 도메인에서 fallback 초기화가 직접 만지는 수치/공격 전략 필드의 책임 축소 가능성을 분류한다.
- Battle/예측/공격 로직으로 이동하지 않는다.

## DEV-028L 소환수 fallback 초기화 공통 적용

- `Summon.FallbackStatusSet`을 추가해 fallback 이름, 등급, 타입, 체력, 공격력 세팅을 `Summon` 본체로 모았다.
- `Summon.AttackStrategiesSet`을 추가해 fallback 일반/특수 공격 전략 연결도 `Summon` 본체 메서드 경로로 모았다.
- 플레이어 소환수 6종과 적/스피릿/보스 소환수 10종이 직접 `summonName`, `maxHP`, `summonRank`, `summonType`, `attackStrategy`, `specialAttackStrategies`를 채우지 않게 정리했다.
- `Snake`는 기존처럼 공격 전략 생성 후 배수를 적용해야 일반공격 전략 데미지 40이 유지되므로 순서를 보존했다.
- 공통 메서드 적용 뒤 파생 클래스가 직접 쓰지 않는 `summonName`, `summonRank`, `summonType`, `maxHP`, `attackStrategy`는 private으로 낮췄다.
- 검증: Unity compile 0 warning, EditMode `Summoner.EditModeTests.SummonDataRegressionTests` 16/16 통과.

## 다음

- 같은 `Summons` 도메인에서 `Cat.SpecialAttack`의 공격력 임시 교체와 공격 가능 상태 변경 책임을 분류한다.
- Battle/예측/공격 로직으로 이동하지 않는다.

## DEV-028M Cat 특수공격 임시 공격력 교체 책임 정리

- `Cat.SpecialAttack`이 직접 `specialAttackStrategies` 배열을 읽고 `attackPower`를 바꿨다가 되돌리던 흐름을 `Summon` protected 메서드로 옮겼다.
- `Summon.SpecialAttackStrategyGet`과 `Summon.SpecialAttackWithAttackPowerExecute`를 추가해 특수공격 전략 조회와 임시 공격력 실행 상태 변경을 본체가 처리한다.
- `Cat`은 특수공격 인덱스/쿨타임 검증과 고유 강공격력 선택만 담당한다.
- 검증: Unity compile 0 warning, EditMode `Summoner.EditModeTests.SummonDataRegressionTests.PlayerSummons_KeepCurrentValuesBeforeDataMigration` 1/1 통과.

## 다음

- 같은 `Summons` 도메인에서 공격 전략 생성 시 직접 읽는 `attackPower`, `heavyAttakPower`를 getter 경로로 바꿀 수 있는지 분류한다.
- Battle/예측/공격 로직으로 이동하지 않는다.

## DEV-028N 소환수 공격력 필드 직접 읽기 정리

- 파생 소환수 16종의 fallback 공격 전략 생성에서 `attackPower`, `heavyAttakPower` 직접 읽기를 `GetAttackPower()`, `GetHeavyAttackPower()` 경로로 바꿨다.
- 파생 클래스의 직접 필드 접근이 사라진 뒤 `Summon.attackPower`, `Summon.heavyAttakPower`, `Summon.isAttack`, `Summon.specialAttackStrategies`를 private으로 낮췄다.
- 정적 확인: 파생 소환수 클래스에는 `Summon` 내부 상태 필드 직접 접근이 남지 않았다.
- 검증: Unity compile 0 warning, EditMode `Summoner.EditModeTests.SummonDataRegressionTests` 16/16 통과.

## 다음

- `Summon.multiple` public field 제거와 `StatMultiplierSet` public API 추가가 포함됐으므로 QAReviewAgent 검증 대상으로 전환한다.
- QA는 `Summons` 도메인 안에서 컴파일, SummonData 회귀, Plate/Summon 정적 회귀 정도로 제한한다.

## QA-028S Summons 잔여 구조 정리 추가 QA 완료

- `Summon.multiple` 직접 접근이 제거되고 Stage 쪽이 `Summon.StatMultiplierSet`으로 배수를 설정하는지 확인했다.
- 파생 소환수 클래스의 fallback 수치/전략 직접 필드 세팅이 `FallbackStatusSet`, `AttackStrategiesSet` 경로로 모였는지 확인했다.
- 공격 버튼 스프라이트, 공격력/체력/보호막/무적/전략 필드 공개 범위 축소 후 외부 접근이 getter/setter 또는 `Summon` 본체 메서드 경로로 유지되는지 확인했다.
- QA 검증: Unity compile 0 warning, EditMode `Summoner.EditModeTests.SummonDataRegressionTests` 16/16, `Summoner.EditModeTests.PlateSummonDrawStaticRegressionTests` 26/26 통과.

## 다음

- FlowAgent가 DEV-028 최신 상태를 정리하고, Summons 도메인 잔여 후보가 끝났는지 한 번 더 확인한다.
- 끝났다면 다음 유지보수 후보를 선정하되 Battle/예측으로 이동하기 전 Summons 잔여 없음 근거를 남긴다.

## DEV-028 최신 상태 완료 정리

- FlowAgent가 Summons 하위 잔여 후보를 다시 확인했다.
- `Summons` 파생 클래스의 단순 override, `Start()` 초기화, 직접 `SummonInitialize()` 호출, fallback 직접 필드 세팅, 공격력/전략 직접 필드 접근은 정리 완료 상태다.
- `Summon.normalAttackSprite`, `specialAttackSprite`, 상태/공격력/전략/보호막/무적/뷰/애니메이션 필드는 private 또는 getter/setter 경로로 닫혔다.
- `Summon.multiple` 직접 접근은 제거됐고 Stage 쪽은 `StatMultiplierSet`으로 배수를 설정한다.
- 남은 공개 필드 후보는 `SummonController.summons`, Stage/UI 인스펙터 필드처럼 이번 `Summons` 본체/파생 소환수 정리 범위를 벗어난 항목이다.
- DEV-028은 QA-028S Pass 기준으로 완료한다.

## 다음

- FlowAgent가 다음 유지보수 후보를 새 작업으로 선정한다.
- 비메서드 네이밍 잔여 정리는 제외 상태를 유지한다.

## DEV-028O Cat 전용 특수공격 우회 위치 재조정

- `Summon.SpecialAttackWithAttackPowerExecute`는 Cat 전용 임시 공격력 교체 우회라 공통 본체 책임으로 두지 않기로 했다.
- 해당 메서드를 `Cat.CatSpecialAttackWithHeavyAttackPowerExecute` private 메서드로 내렸다.
- `Summon`에는 Cat 전용 `SpecialAttackStrategyGet`, `SpecialAttackWithAttackPowerExecute` protected 메서드가 남지 않는다.
- 검증: Unity compile 0 warning, EditMode `Summoner.EditModeTests.SummonDataRegressionTests.PlayerSummons_KeepCurrentValuesBeforeDataMigration` 1/1 통과.

## DEV-029 다음 작업 선정: 공격 전략 damage source 정리

- 사용자가 다음 작업을 데미지 쪽으로 지정했다.
- 목표는 `ClosestEnemyAttackStrategy.damage`와 실제 피해 적용 기준을 명시해 Cat 특수공격 우회 제거 가능성을 검토하는 것이다.
- 일반 공격처럼 버프/저주가 반영되어야 하는 공격은 `attacker.GetAttackPower()` 기준을 유지한다.
- 전략 damage를 실제 피해로 써야 하는 공격과 공격자 현재 공격력을 써야 하는 공격을 구분하는 방향으로 분석한다.
- 첫 단계는 코드 변경 없이 `ClosestEnemyAttackStrategy`, `ClosestEnemyAttackEffectInstanceCreate`, `Cat.SpecialAttack`, `CatAttackPrediction` 흐름을 분류한다.
