# QA-025 - 전투 공격 실행 흐름 분리 분석

대상: DEV-025 전투 공격 실행 흐름 분리 분석

## 기대 동작

- `BattleController`가 특수공격 대상 plate 선택을 공통 helper로 처리한다.
- 플레이어/적, 이로운/해로운 특수공격의 대상 plate 매핑은 기존과 같다.
- `SpecialAttackExecute` public API와 `Summon.SpecialAttack` 호출 계약은 유지된다.

## 확인 방법

- 변경 diff 정적 검토
- Unity script recompile
- EditMode `Summoner.EditModeTests.PlateSummonDrawStaticRegressionTests`

## 결과

- `SpecialAttackTargetPlatesGet`은 `BenefitEffectCheck()`와 `isPlayer` 기준으로 기존 분기와 같은 대상 plate를 반환한다.
- `Targeted`, `AllEnemies`, `ClosestEnemy` 특수공격 실행은 기존 `SpecialAttackApply` 흐름을 유지한다.
- Unity compile 0 warning.
- `Summoner.EditModeTests.PlateSummonDrawStaticRegressionTests` 24/24 통과.

결과: Pass

## 다음 요청

- FlowAgent가 DEV-025 완료를 정리하고 다음 작은 구현 후보를 선정한다.
