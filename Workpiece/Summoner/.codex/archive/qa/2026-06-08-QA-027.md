# QA-027 - 적 반응 특수공격 실행 로그 중복 분리

대상: DEV-027 적 반응 특수공격 실행 로그 중복 분리

## 기대 동작

- `EnermyAlgorithm`의 적 특수공격 실행과 로그 출력 반복이 private helper로 분리된다.
- 특수공격 대상 plate index, 특수공격 배열 index, 로그 문구는 기존과 같다.
- public API, 예측 알고리즘, 공격 확률은 변경하지 않는다.

## 확인 방법

- 변경 diff 정적 검토
- Unity script recompile
- EditMode `Summoner.EditModeTests.PlateSummonDrawStaticRegressionTests`

## 결과

- `EnemySpecialAttackExecute`는 기존 `battleController.SpecialAttackExecute` 인자와 로그 문구를 그대로 받는다.
- `EnermyAlgorithm` 내부 직접 특수공격 실행 호출은 helper 안으로 모였다.
- Unity compile 0 warning.
- `Summoner.EditModeTests.PlateSummonDrawStaticRegressionTests` 26/26 통과.

결과: Pass

## 다음 요청

- FlowAgent가 DEV-027 완료를 정리하고 다음 작은 구현 후보를 선정한다.
