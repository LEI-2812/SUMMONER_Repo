# QA-032

## QA Smoke Result

대상:
DEV-062 공격 대상 방식과 상태 효과 분리

기대 동작:
- 공격 대상 선택 책임은 `IAttackTargetSelector` 구현 쪽에서 드러난다.
- Shield/OnceInvincibility 자기 대상 효과는 effect 내부에서 `target = attacker`로 우회하지 않는다.

확인 방법:
- Unity MCP EditMode `Summoner.EditModeTests.SummonFeedbackSeparationTests`

QA 깊이 기준:
- `AttackTargetSelection_IsSeparatedFromStatusEffect` 회귀 테스트 통과

결과: Pass

확인한 내용:
- `SummonFeedbackSeparationTests`: 9/9 passed, 0 failed, 0 skipped
- `AttackTargetSelection_IsSeparatedFromStatusEffect` 통과

문제:
- 없음

다음 요청:
- CoordinatorAgent가 DEV-063 대상 판정 책임 경계를 확정한다.
