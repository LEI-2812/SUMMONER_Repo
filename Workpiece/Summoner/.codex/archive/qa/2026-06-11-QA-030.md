# QA-030

## QA Smoke Result

대상: DEV-055 `SummonAttackData` Get prefix getter 계약

기대 동작:
- `SummonAttackData` public getter가 `Get` prefix 이름으로 제공된다.
- legacy `StrategyTypeGet`, `StatusTypeGet`, `DamageGet`, `CooltimeGet`, `StatusTimeGet` 중복 getter가 남지 않는다.
- 기존 공격 전략 생성 동작은 유지된다.

확인 방법:
- Unity MCP `recompile_scripts`
- Unity MCP EditMode `Summoner.EditModeTests.SummonDataRegressionTests`

QA 깊이 기준:
- script compile warning 0
- `SummonDataRegressionTests`에서 getter 계약과 공격 전략 생성 회귀 테스트 통과

결과: Pass

확인한 내용:
- script compile: 0 warning
- EditMode `SummonDataRegressionTests`: 18/18 passed, 0 failed, 0 skipped
- `SummonAttackData_ProvidesGetPrefixGetterNames`, `SummonAttackData_DoesNotKeepDuplicateGetterNames`, `SummonAttackData_CreatesCurrentAttackStrategies` 통과

문제:
- 없음

다음 요청:
- CoordinatorAgent가 같은 `Summons/Data` 도메인에서 `SummonData` getter 명명 정리를 다음 DEV 작업으로 확정한다.
